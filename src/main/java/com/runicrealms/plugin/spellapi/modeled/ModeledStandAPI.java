package com.runicrealms.plugin.spellapi.modeled;

public interface ModeledStandAPI {

    /**
     * Adds a custom ModeledItem to its manager
     *
     * @param modeledStand to add
     */
    void addModeledStandToManager(ModeledStand modeledStand);
}
