package com.runicrealms.plugin.spellapi.modeled;

public interface ModeledSpellAPI {

    /**
     * Adds a custom ModeledSpell to the manager
     *
     * @param modeledSpell to add
     */
    void addModeledSpellToManager(ModeledSpell modeledSpell);
}
