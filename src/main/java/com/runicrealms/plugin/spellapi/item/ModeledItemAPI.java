package com.runicrealms.plugin.spellapi.item;

public interface ModeledItemAPI {

    /**
     * Adds a custom ModeledItem to its manager
     *
     * @param modeledItem to add
     */
    void addModeledItemToManager(ModeledItem modeledItem);
}
